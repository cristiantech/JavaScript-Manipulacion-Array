const letter = ["a","b","c","d","e"];
const newMap = letter.map(res => `${res} ++`)

// const newArray = [];
// for (let index = 0; index < letter.length; index++) {
//     const res = letter[index];
//     newArray.push(`${res} ++`);
// }

console.log(`newArray ${newMap}`);